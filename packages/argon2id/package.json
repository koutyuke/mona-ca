{
  "name": "@mona-ca/argon2id",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "cargo:build": "wasm-pack build",
    "check": "biome check --error-on-warnings --no-errors-on-unmatched",
    "check:fix": "bun run check --write",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@mona-ca/tsconfig": "workspace:*"
  }
}
